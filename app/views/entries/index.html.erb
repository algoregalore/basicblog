<h1>this is a blog</h1>

<%= link_to "about page", about_main_index_path %>


<% @entries.each.reverse_each do |entry| %>
  <h3><%= entry.title %></h3>
  posted<strong> <%= entry.created_at.localtime.stamp("July 1, 2000 at 4:00pm") %></strong> <%= link_to 'edit', edit_entry_path(entry) %> <%= link_to 'show', entry_path(entry) %>

  <p><%= entry.content %></p>

  <p>~~~~~</p>

<% end %>

<p><%= link_to "add a new entry", new_entry_path %>